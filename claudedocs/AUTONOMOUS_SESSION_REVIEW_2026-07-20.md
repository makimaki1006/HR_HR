# 自律実装セッション レビュー (2026-07-20 深夜〜早朝)

ユーザー就寝中の自律実装の全記録。**全変更はpush済み・本番検証済み**。

## 成果サマリー

| コミット | 内容 | 本番検証 |
|---|---|---|
| `ce004ff` | **Phase 2a: カード単位事実** — CardBrief(給与欄形式/休日/新着/説明文給与)を全レコード分集計し、解説資料の依頼企業カード解析に使用 | ✅ |
| `3f3f484` | **アプリ改善2件** — ①CSVソース既定を「自動判定(推奨)」に(SP選び忘れ→§07.5消失事故の恒久対策) ②給与レンジ反転(min>max)のswapバリデーション(既知バックログ) | ✅ 既定=auto実測 |
| `bab1aee` | フルビルド破損修正(gen_vrt_fixtures) — 下記「教訓」参照 | ✅ デプロイ回復 |
| `6b644db` | **SP訴求スニペット列(css-lx9x6g)のマッピング** — 「月収35万円以上も可能」等の訴求はdescriptionでなくこの列にあった。年間休日抽出には未使用(§07.5数値は不変) | ✅ 8/8チェック |

テスト: 全2314本合格 / lint_statistical_claims 0件 / フルワークスペースreleaseビルド成功。

## 最終成果物 (Downloads)

**`求人市場レポート 解説資料【富田林市　建設業】AI版v4_カード完全版.pdf/.html`**

CSVアップロード+企業名入力だけで、手作りv4の核心指摘が自動生成されるようになった:

> 観測(コード確定): カード「【正社員】設備の金属コーティング技術者」: 給与欄は単一値 25.0万円 (幅の表示なし)。説明文中には月収35万円の記載があるが、給与欄の上限には反映されていない。年間休日の記載 126 日 (市場で125日以上を明示する求人は 18% — この上位帯に入る)。新着表示はない。
>
> だから(flash-lite起草+逆証明レビュー+機械検証): 市場の約79%が給与に幅を持たせている中で、貴社は単一値で表記しています。説明文中の月収35万円を給与欄の上限に反映させることで、市場水準との乖離を是正し、応募意欲を高めることが検討候補になります。

判定ロジック(単一値/乖離/休日帯)はすべてRustで確定し、LLMは解釈文のみ。数値出所ガードで捏造は機械的に遮断。

## 本番E2E検証(最終ラウンド、8/8合格)

1. **ラジオ無操作アップロード**で自動判定が働き§07.5(休日53%)が出る(改善①の実地検証)
2. 貴社求人カードのセクション出現
3. 給与欄単一値の指摘
4. 説明文35万円の乖離指摘
5. 126日の記載
6. 125日以上上位帯の位置づけ
7. 複合考察(AI版の指標)
8. 禁止語ゼロ

## 教訓(検証手順の改善、自己修正済み)

Phase 2aのpush後、Renderデプロイが約40分反映されず調査した結果、**`cargo test --lib`のみで全バイナリのビルドを回さずpushした私の検証手順の穴**を検出(gen_vrt_fixturesバイナリの全フィールドリテラルがcard_briefs欠落でコンパイル失敗→Renderフルビルド失敗)。`bab1aee`で修正し、以後の検証をフルワークスペース`cargo build --release`+全テストに切り替えた。部分コミット検証ルール(ローカル成功≠本番成功)の新しい実例。

## 未着手・判断待ち

- **APIキーのロール分割**(3プロジェクト構成の提案済み) — ユーザー判断待ち
- Gemini APIキーのローテーション(7/13にチャット履歴へ露出した件) — キー分割と同時実施を推奨
- 案B(コンサル商談準備レポートへのCSVセッション接続) — Phase 2完了により素材は揃った
- 「優先アクション表 (仮)」の表記 — 経営サマリーと同様に除去するか確認待ち
