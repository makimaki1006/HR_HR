# Build artifacts
target/
*.exe

# Environment
.env

# Database files (DB is downloaded from GitHub Release at build time)
data/*.db
data/*.db.gz
# Phase 3: 再生成可能な集計CSV (scripts/build_*.py で再現可能、サイズ大)
data/generated/

# Uncompressed GeoJSON (decompressed from data/geojson_gz/ at startup)
static/geojson/

# Old git backup
.git_backup/

# OS files
.DS_Store
Thumbs.db
nul

# Windows-specific cargo config
.cargo/

# Build scripts (development only)
build_release.bat

# Scripts: compute_v2_*.py are tracked (pre-computation for analysis)
scripts/*.bat
scripts/*.sh
!scripts/download_db.sh
!scripts/setup.sh
!scripts/run_all_e2e.sh
scripts/!compute_v2_*.py

# Dev docs
GAP_ANALYSIS.md
STATUS.md

# === E3 (2026-04-26) workspace hygiene ===

# E2E test artifacts (auto-generated screenshots)
*.png
chart_verify*.png
check_*.png
d??_*.png
# 例外: ドキュメント・ガイド用 PNG はコミット対象
!docs/screenshots/*.png
!docs/audit_*/*.png
!static/guide/*.png

# Test mocks (CSV upload tests)
_*_mock.csv
_sec_tmp/

# Backups
*.bak
*.old

# Coverage / lcov
*.profraw
target/llvm-cov/

# Python bytecode
__pycache__/
*.pyc

# Node.js / Playwright (E2E)
node_modules/
playwright-report/
test-results/
.playwright/

# === Round 4 / E2E generated artifacts (2026-05-06) ===
# SQLite ジャーナル/WAL/SHM (DB 操作中に生成される一時ファイル)
*.db-wal
*.db-shm
*.db-journal
# ログ・一時ファイル
*.log
*.tmp
# E2E スクリーンショット (ルート直下のみ。docs/audit_*/*.png は上の例外で保護済)
/e2e_*.png
/verify_*.png
/theme_*.png
# 印刷検証 PDF
report_*_print.pdf
tests/e2e/tmp_pdf_*.pdf
# Playwright trace / 動画
trace.zip
videos/

# === Round 10 worktree cleanup (2026-05-11) ===
# E2E 検証 PDF / HTML / 監査出力の置き場 (再生成可)
out/
# 補助的な cargo target ディレクトリ (target-test/、target-e2e-print-*/ など)
target-*/
# pytest cache
.pytest_cache/
# ad-hoc サーバ起動の標準出力/エラー
server.err
server.out
# 生成系 CSV / progress (build_*.py で再現可能)
scripts/data/
# Turso upload の補助ログ (再現可)
data/agoop/logs/
# レポート試作物 (HTML mockup、必要なら docs/ に移動)
report_mockups/
.claude/.audit_numeric_done

# Local-only test fixtures & scripts (contain credentials/customer data)
_tmp_pdf_test.mjs
.playwright-mcp/
