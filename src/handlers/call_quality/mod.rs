//! 架電クオリティ（GAS 版ダッシュボードの移植先）
//!
//! 2026-08-14 開始。GAS 版は 16タブ / 65シート / JS 19,582行。
//! 移植方針はユーザー決定により **全写し**。
//!
//! 併存期間の扱い（ユーザー決定）:
//!   ログインは HR_HR 既存のものを共通で使うが、
//!   **GAS 版とは全く別のページとして扱う**（同じ画面に混在させない）。
//!
//! 構成:
//!   sheets.rs   65シート共通のアクセス層（常駐キャッシュ + サーバ側の絞り込み/集計）
//!   heatmap.rs  時間帯ヒートマップ（サーバ側集計の PoC。実測 33.1MB → 6.7KB）

pub mod heatmap;
pub mod sheets;
