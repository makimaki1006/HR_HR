# Round 9 P2-D': 業界別最賃比中央値 整備可否監査

**日付**: 2026-05-10
**判断**: **案 B (注記追加のみ) 採用** — wage.rs に「業界横断比較ではない」を明記

---

## 結論 (3 行)

1. CSV に業界列なし + 業界推定禁止方針 = CSV ↔ 業界別給与の **構造的に結合不可能**
2. 業界×給与中央値を持つテーブル (v2_salary_structure 等 4 件) はすべて **HW 由来** = MI variant 混入禁止
3. 既存 `wage.rs` Severity badge は維持、注記追加のみで説明責任達成

---

## 1. 監査結果サマリ

### 1-1. 業界別給与データの調達可能性

| データ源 | 業界粒度 | 月給中央値 | 結合可能性 | MI 利用可? |
|---|---|---|---|---|
| HW `postings.industry_raw` (469,027 行 / 100% 充足) | 小分類 | salary_min/max | 自治体結合可 | ❌ HW 由来 |
| HW 派生 `v2_salary_structure` (17,380 行) | 小分類 | p25/p50/p75/p90 完備 | 同上 | ❌ HW 由来 |
| HW 派生 `v2_shadow_wage` (12,136 行) | 小分類 | p10〜p90 完備 | 同上 | ❌ HW 由来 |
| 経済センサス R3 `v2_external_industry_structure` (36,099 行) | 大分類 + 中分類 | **無し** | — | ⚪ ただし給与なし |
| e-Stat 賃金構造基本統計調査 | 大/中分類 + 職業別 | あり | 都道府県粒度のみ | ⚪ 取得すれば可、ただし未実装 |

→ 業界 × 給与中央値を持つテーブルは **すべて HW 派生**。MI variant では使えない。

### 1-2. 結合不能性の証明

CSV 側 `SurveyAggregation.by_prefecture_salary` には **業界カラムなし** (`industry_salary.rs:7-30`)。会社名/職種語彙からの業界推定は Round 3-C/3-C' で「方針として死亡」、本ラウンドで再禁止。→ **CSV × 業界の結合は構造的に不可能**。

---

## 2. 採用方針: 案 B (注記追加のみ)

### 実装変更 (`src/handlers/survey/report_html/wage.rs:370-388`)

「活用ポイント」div 内に追加:

```html
<strong>本指標の範囲:</strong> 最賃比は対象地域の最低賃金との距離を示す指標であり、
<strong>業界横断比較・職種横断比較の指標ではありません</strong>。
CSV 給与中央値は媒体掲載求人の混合値で、業界別・職種別の中央値とは粒度が異なります。
業界別最賃比中央値は本レポートでは作成していません
(CSV に業界列がなく、推定で断定するのは採用判断として不適切なため)。
```

### 不採用案 (Agent D' 評価結果)

| 案 | 理由 |
|---|---|
| 案 A: 表示なし (docs のみ) | 既存 wage.rs に注記追加で誤解防止できる、価値ゼロは惜しい |
| 案 C: 業界別最賃比表追加 | Full variant 限定で HW 派生 OK だが、MI variant 混入禁止に抵触 + スクリプト/render/テスト = 大幅実装、本ラウンドのスコープ超過 |

---

## 3. 残課題 (将来余地)

| ID | 内容 | 優先 |
|---|---|---|
| Round X | Full variant 限定で `v2_salary_structure` の小分類 industry_raw × prefecture × emp_group 中央値を独立表 (HW スコープ caveat 必須) | 低 |
| Round X | e-Stat 賃金構造基本統計を新規 fetch + 都道府県 × 大分類業界 中央値を作成 (自治体粒度なし、汎用統計) | 中 |

---

## 4. 監査メタデータ

- 修正ファイル: 1 件 (wage.rs、コメント + 1 段落追加)
- DB 書込: ゼロ
- 新規 fetch: なし
- HW 由来データの MI 混入: なし

**Round 9 P2-D' は注記追加で完了。本番 PDF で「業界横断比較ではない」表示確認で最終完了。**
